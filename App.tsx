import { SafeAreaView, ScrollView, Text } from "react-native";

export const App = () => (
  <SafeAreaView>
    <ScrollView>
      <Text>{"Hello Rizna team!!!"}</Text>
    </ScrollView>
  </SafeAreaView>
);

export default App;
